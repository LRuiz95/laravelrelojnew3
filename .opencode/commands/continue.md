---
description: resume work from the recorded continuation state with safety gates
agent: team-lead
model: opencode/big-pickle
---

Resume from `.opencode/team/CONTINUE.md` and current repository state for: $ARGUMENTS. Re-run Git/impact safety checks if the repository changed since the last state. Execute the recommended next step unless a new risk invalidates it; then re-plan. Finish with updated continuation options.
