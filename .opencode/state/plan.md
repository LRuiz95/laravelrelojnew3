# Plan

> Template vacío. Se llena al inicio de cada tarea con el plan de
> implementación (obligatorio en tareas COMPLEJAS, opcional recomendado
> en SIMPLES). Incluye: problema, causa raíz, módulos afectados,
> dependencias entre pasos, riesgos, rollback y nivel de testing.

(sin plan registrado para la tarea activa)