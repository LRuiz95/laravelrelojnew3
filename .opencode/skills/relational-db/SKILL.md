---
name: relational-db
description: Analyze relational schemas, SQL, migrations, indexes, constraints, query plans and data integrity.
compatibility: opencode
---
# Relational Database

Inspect the actual schema before changing SQL.

For queries, consider predicates, joins, cardinality, indexes, sorting, grouping and pagination.

For schema changes, define migration order, constraints, rollback strategy and compatibility with existing application versions.
